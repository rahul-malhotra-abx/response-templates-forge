{
  "name": "response-templates-editor",
  "version": "0.0.1",
  "private": true,
  "description": "Placeholder editor served from the app's own static resources, standing in for jira-editor.appbox.ai while we test whether Forge allows a same-origin nested iframe.",
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --minify --loader:.jsx=jsx --outfile=../static/forge-angular-app/src/assets/editor/static/js/main.js && cp public/index.html ../static/forge-angular-app/src/assets/editor/index.html"
  },
  "dependencies": {
    "iframe-channel": "^1.2.10",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
